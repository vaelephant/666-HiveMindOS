# 审计日志 — 全链路行为追溯

> M3 里程碑：记录 AI 任务执行、Wiki 变更、对外通信与质量巡检，供合规与运营审查。

---

## 一句话

**审计日志回答「AI 做了什么、改了什么、发给谁」** — 与 Token 用量统计（`/account`）互补。

---

## 记录范围

| 分类 | 触发点 | 示例 action |
|------|--------|-------------|
| **task** | 自主任务 Executor 每步执行 | `task.llm_generate`、`task.search_wiki` |
| **wiki** | 候选编译进 Wiki | `wiki.compile` |
| **candidate** | 人工批准 / 驳回候选 | `candidate.approve`、`candidate.reject` |
| **communicate** | 企微出站消息 | `wechat.send` |
| **deliverable** | 任务保存交付物 | `task.save_deliverable` |
| **lint** | Wiki 质量巡检自动化 | `wiki.lint` |

失败步骤记 `status=error`，不阻断主流程。

---

## API

Base：`http://localhost:8006/api/v1`（WebUI 经 `/api/kb/{orgId}/audit/events` 代理）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/orgs/{org}/audit/events` | 事件列表 + 统计摘要 |
| GET | `/orgs/{org}/audit/stats` | 仅统计 |

查询参数：`days`（1–365）、`category`、`action`、`limit`、`offset`、`user_id`

```bash
curl "http://localhost:8006/api/v1/orgs/demo/audit/events?days=7&category=task"
```

---

## 数据库

迁移：`db/migrations/012_audit_events.sql`

```bash
cd backend && python scripts/migrate_db.py
```

---

## 前端

- 路由：`/audit`
- 组件：`webui/src/components/audit/audit-log-view.tsx`

---

## 相关配置

| 文件 | 说明 |
|------|------|
| `agent_engine/settings/task_gates.yaml` | `action_gates.wechat_work_send: human` — 企微发送需人工批准 |
| `knowledge_base/settings/automations.yaml` | `lint_wiki` — Wiki 巡检 cron，结果写入审计 |

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-06-13 | 初稿：audit_events 表、服务、API、WebUI、任务/候选/企微钩子 |
